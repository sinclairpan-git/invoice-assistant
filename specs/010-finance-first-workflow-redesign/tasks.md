---
related_doc:
  - "docs/invoice-assistant-gap-backlog.zh-CN.md"
  - "docs/superpowers/specs/2026-04-24-invoice-assistant-ux-redesign.md"
  - "specs/010-finance-first-workflow-redesign/spec.md"
  - "specs/010-finance-first-workflow-redesign/plan.md"
---
# 任务分解：财务优先工作流重设计
**编号**：`010-finance-first-workflow-redesign` | **日期**：2026-04-24
**来源**：`spec.md` + `plan.md`

---

## 分批策略

```text
Batch 1: 正式规格草案与对抗评审收口
Batch 2: Phase 0 体验止血
Batch 3: Phase 1 契约冻结与兼容矩阵
Batch 4: Phase 2 配置中心重做
Batch 5: Phase 3 批次结果、人工确认与可用预览闭环
Batch 6: Phase 4 导出主流程与兼容收口
```

---

## Batch 1：正式规格草案与对抗评审收口

### Task 1.1 起草 010 正式范围与非目标

- **任务编号**：`T11`
- **优先级**：P0
- **依赖**：无
- **文件**：`spec.md`, `plan.md`, `tasks.md`, `task-execution-log.md`
- **可并行**：否
- **验收标准**：
  1. `spec.md` 明确起草“配置中心重做 + 批次结果重做 + 人工确认闭环 + 导出主流程收口”的范围。
  2. 文档明确“不重写 OCR 算法、不回写历史批次、不做权限系统”这些非目标。
- **验证**：文档对读 + `python -m ai_sdlc workitem close-check --wi specs/010-finance-first-workflow-redesign --json`
- **状态**：已完成

### Task 1.2 完成两名对抗 agent 合议评审

- **任务编号**：`T12`
- **优先级**：P0
- **依赖**：`T11`
- **文件**：`spec.md`, `plan.md`, `tasks.md`, `task-execution-log.md`
- **可并行**：是
- **验收标准**：
  1. “财务小白但强产品/财务视角”与“财务系统技术专家”两名 agent 都完成审阅。
  2. 阻断级分歧被收敛，正式文档只保留双方都能接受的方案。
- **验证**：agent 审阅记录 + 文档修订记录
- **状态**：已完成

## Batch 2：Phase 0 体验止血

### Task 2.1 移除后续配置 JSON 主入口

- **任务编号**：`T21`
- **优先级**：P0
- **依赖**：`T12`
- **文件**：`frontend/src/pages/Settings.tsx`, `frontend/src/components/settings/*`
- **可并行**：否
- **验收标准**：
  1. 配置中心主路径不再展示 JSON 编辑器。
  2. 用户能从配置中心进入字段编辑视图。
- **验证**：前端测试 + Playwright
- **状态**：已完成

### Task 2.2 结果页与详情页默认收起内部诊断

- **任务编号**：`T22`
- **优先级**：P0
- **依赖**：`T12`
- **文件**：`frontend/src/pages/BatchResults.tsx`, `frontend/src/components/results/*`
- **可并行**：是
- **验收标准**：
  1. 主表与详情首屏使用财务动作语言。
  2. `risk_flags`、provider 诊断等仅保留在高级信息区域。
- **验证**：前端测试 + Playwright
- **状态**：已完成

### Task 2.3 修复处理中等待态与人工确认即时刷新

- **任务编号**：`T23`
- **优先级**：P0
- **依赖**：`T12`
- **文件**：结果页、详情页、复核相关前后端模块
- **可并行**：是
- **验收标准**：
  1. 解析未完成前显示处理中，不秒报税号缺失。
  2. 人工确认后列表、详情、摘要立即刷新。
- **验证**：Playwright + pytest
- **状态**：已完成

## Batch 3：Phase 1 契约冻结与兼容矩阵

### Task 3.1 冻结 ConfigBundle 的唯一事实源与版本规则

- **任务编号**：`T31`
- **优先级**：P0
- **依赖**：`T21`
- **文件**：`spec.md`, `plan.md`, 配置相关 API/schema 文档与实现
- **可并行**：否
- **验收标准**：
  1. `ConfigBundle` 被明确为唯一主写事实源。
  2. 统一 bundle 版本号、旧三版本映射规则和批次快照读取规则被正式冻结。
- **验证**：文档对读 + schema/接口测试
- **状态**：已完成

### Task 3.2 冻结稳定状态模型、归档语义与兼容矩阵

- **任务编号**：`T32`
- **优先级**：P0
- **依赖**：`T31`
- **文件**：`spec.md`, `plan.md`, 状态相关 API/schema 文档与实现
- **可并行**：否
- **验收标准**：
  1. `processing_status`、`review_status`、`archive_status` 的枚举与状态转移被正式冻结。
  2. `archive_status` 明确表示业务可另存为与显式另存为历史，不表示自动落盘。
  3. 兼容矩阵明确旧 `display_status`、旧导出、旧配置 JSON 的读写角色和退场条件。
- **验证**：文档对读 + schema/接口测试
- **状态**：已完成

### Task 3.3 冻结人工确认工作项的最小领域契约

- **任务编号**：`T33`
- **优先级**：P1
- **依赖**：`T32`
- **文件**：`spec.md`, `plan.md`, 复核相关 API/schema 文档与实现
- **可并行**：是
- **验收标准**：
  1. 明确 `ReviewQueueItem` 为持久化工作项，且同一批次同一发票最多只有一个 `open` 工作项。
  2. 明确入队条件、出队条件、排序规则、并发冲突和幂等要求。
  3. 明确复核提交后返回更新后的单票数据和权威 `BatchActionSummary`。
- **验证**：文档对读 + schema/接口测试
- **状态**：已完成

## Batch 4：Phase 2 配置中心重做

### Task 4.1 抽取共享字段表单组件

- **任务编号**：`T41`
- **优先级**：P0
- **依赖**：`T31`, `T32`
- **文件**：`SetupWizard.tsx`, `TaxProfileStep.tsx`, `Settings.tsx`, `frontend/src/components/settings/*`
- **可并行**：否
- **验收标准**：
  1. 首次配置与后续编辑复用同一套字段组件。
  2. 公司税务档案、审核策略、文件命名都可通过字段方式编辑。
- **验证**：组件测试 + Playwright
- **状态**：未开始

### Task 4.2 实现配置发布确认与历史变更只读视图

- **任务编号**：`T42`
- **优先级**：P1
- **依赖**：`T41`
- **文件**：配置中心页面、配置 API、配置服务
- **可并行**：否
- **验收标准**：
  1. 保存前看到摘要、原因、差异与影响范围。
  2. 历史版本只读显示，不允许直接编辑历史内容。
- **验证**：前端测试 + API 测试
- **状态**：未开始

## Batch 5：Phase 3 批次结果、人工确认与可用预览闭环

### Task 5.1 重构结果页为财务动作分组

- **任务编号**：`T51`
- **优先级**：P0
- **依赖**：`T22`, `T23`, `T32`
- **文件**：`BatchResults.tsx`, `ResultTable.tsx`, 相关前端状态管理
- **可并行**：否
- **验收标准**：
  1. 顶部摘要与筛选都按“建议通过 / 待人工确认 / 本批次重复 / 需补充/重试”组织。
  2. 主表支持另存为当前筛选结果与勾选结果。
- **验证**：Playwright
- **状态**：未开始

### Task 5.2 建立独立的待人工确认工作台

- **任务编号**：`T52`
- **优先级**：P0
- **依赖**：`T23`, `T33`
- **文件**：新页面与复核相关组件/API
- **可并行**：是
- **验收标准**：
  1. 默认只显示待人工处理票据。
  2. 支持列表切票、详情判断和复核记录显示。
- **验证**：Playwright + pytest
- **状态**：未开始

### Task 5.3 固化“本批次重复”判定边界

- **任务编号**：`T53`
- **优先级**：P0
- **依赖**：`T51`
- **文件**：批次服务、重复判定逻辑、回归测试
- **可并行**：是
- **验收标准**：
  1. 新批次不看历史判重。
  2. 当前批次内部重复仍然保留。
- **验证**：pytest 集成测试
- **状态**：未开始

### Task 5.4 提供稳定预览支撑人工确认

- **任务编号**：`T54`
- **优先级**：P0
- **依赖**：`T33`
- **文件**：预览相关前后端组件/API
- **可并行**：是
- **验收标准**：
  1. 人工确认工作台上线时已具备稳定 PDF 预览和原始文件入口。
  2. 用户不需要盲审即可做出确认动作。
- **验证**：Playwright + API 测试
- **状态**：未开始

## Batch 6：Phase 4 导出主流程与兼容收口

### Task 6.1 统一主流程为另存为下载

- **任务编号**：`T61`
- **优先级**：P1
- **依赖**：`T51`, `T53`
- **文件**：结果页导出组件、导出服务、相关 API
- **可并行**：否
- **验收标准**：
  1. UI 主流程不再自动落盘。
  2. 支持另存为筛选结果、勾选结果和台账导出。
- **验证**：Playwright + API 测试
- **状态**：未开始

### Task 6.2 清理旧兼容层并收口预览/状态遗留入口

- **任务编号**：`T62`
- **优先级**：P1
- **依赖**：`T54`, `T61`
- **文件**：`invoices.py`, `status_service.py`, 兼容映射层与相关前端入口
- **可并行**：是
- **验收标准**：
  1. 旧 HTML 预览与旧 `display_status` 不再作为默认主入口。
  2. 兼容层仅保留必要只读映射和回退路径。
- **验证**：Playwright + API 测试
- **状态**：未开始
